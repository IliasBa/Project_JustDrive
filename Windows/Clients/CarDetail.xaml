<Window x:Class="Project_JustDrive.Windows.Clients.CarDetail"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Just Drive - Auto detail"
        Height="800" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="#F5F6F8">

    <Window.Resources>
        <SolidColorBrush x:Key="Accent" Color="#F26B1F"/>
        <SolidColorBrush x:Key="AccentHover" Color="#D85A14"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#0F1B2D"/>
        <SolidColorBrush x:Key="TextMuted" Color="#6B7280"/>
        <SolidColorBrush x:Key="BorderLight" Color="#E5E7EB"/>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Accent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource AccentHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#F0F1F4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="60,40">

            <!-- Terug knop -->
            <Button Content="← Terug naar overzicht" Style="{StaticResource SecondaryButton}"
                    HorizontalAlignment="Left" Margin="0,0,0,30"
                    Click="BtnTerug_Click"/>

            <!-- Auto naam -->
            <TextBlock x:Name="TxtNaam" FontSize="32" FontWeight="Bold"
                       Foreground="{StaticResource TextPrimary}" Margin="0,0,0,4"/>
            <TextBlock x:Name="TxtType" FontSize="16" Foreground="{StaticResource TextMuted}"
                       Margin="0,0,0,30"/>

            <!-- Afbeelding + details -->
            <Grid Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="360"/>
                </Grid.ColumnDefinitions>

                <!-- Afbeelding placeholder -->
                <Border Grid.Column="0" Height="500" Background="#1F2937"
        CornerRadius="12" Margin="0,0,20,0"
        ClipToBounds="True">
                    <Image x:Name="ImgCar" Stretch="UniformToFill" 
           VerticalAlignment="Center"
           HorizontalAlignment="Center"/>
                </Border>

                <!-- Details kaart -->
                <Border Grid.Column="1" Background="White" CornerRadius="12" Padding="28">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.06" BlurRadius="20" ShadowDepth="2"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Details" FontSize="18" FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimary}" Margin="0,0,0,16"/>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Brandstof" Foreground="{StaticResource TextMuted}"/>
                            <TextBlock x:Name="TxtBrandstof" Grid.Column="1" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimary}"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Transmissie" Foreground="{StaticResource TextMuted}"/>
                            <TextBlock x:Name="TxtTransmissie" Grid.Column="1" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimary}"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Borg" Foreground="{StaticResource TextMuted}"/>
                            <TextBlock x:Name="TxtBorg" Grid.Column="1" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimary}"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Verbruik/100km" Foreground="{StaticResource TextMuted}"/>
                            <TextBlock x:Name="TxtVerbruik" Grid.Column="1" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimary}"/>
                        </Grid>

                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Nummerplaat" Foreground="{StaticResource TextMuted}"/>
                            <TextBlock x:Name="TxtNummerplaat" Grid.Column="1" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimary}"/>
                        </Grid>

                        <!-- Prijs -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <TextBlock x:Name="TxtPrijs" FontSize="28" FontWeight="Bold"
                                       Foreground="{StaticResource Accent}"/>
                            <TextBlock Text=" / dag" FontSize="16" Foreground="{StaticResource TextMuted}"
                                       VerticalAlignment="Bottom" Margin="4,0,0,4"/>
                        </StackPanel>

                        <!-- Datums -->
                        <TextBlock Text="Ophaaldatum" Foreground="{StaticResource TextMuted}" Margin="0,0,0,4"/>
                        <DatePicker x:Name="DpStart" Margin="0,0,0,12"/>

                        <TextBlock Text="Retourdatum" Foreground="{StaticResource TextMuted}" Margin="0,0,0,4"/>
                        <DatePicker x:Name="DpEnd" Margin="0,0,0,20"/>

                        <!-- Totaalprijs -->
                        <TextBlock x:Name="TxtTotaal" FontSize="16" FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimary}" Margin="0,0,0,16"/>

                        <Button Content="Reserveer nu" Style="{StaticResource PrimaryButton}"
                                Click="BtnReserveer_Click"/>
                        <Button x:Name="BtnFavoriet" Content="♥  Toevoegen aan favorieten" 
                                Style="{StaticResource SecondaryButton}"
                                Margin="0,10,0,0"
                                Click="Favoriet_Click"/> 
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Window>